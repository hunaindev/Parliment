# org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
# android.useAndroidX=true
# android.enableJetifier=true


# Set a reasonable memory limit for Gradle.
# We are changing the 4G from the error log to 2G.
org.gradle.jvmargs=-Xmx2048M 

android.useAndroidX=true
android.enableJetifier=true


# Set a specific memory limit for the Gradle Daemon JVM
# org.gradle.jvmargs=-Xmx2g

# # Enable modern features that can help with stability
# org.gradle.parallel=true
# org.gradle.caching=true



# The Final Fix: Reduce memory slightly and disable aggressive parallelism.
# This makes the build slower but significantly more stable on systems under memory pressure.
# org.gradle.jvmargs=-Xmx1536m
# org.gradle.parallel=false